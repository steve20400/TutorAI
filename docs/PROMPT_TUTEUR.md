# Prompt du tuteur

Le prompt système est le fichier lu par l'application à l'exécution :

**[`src/data/prompt-tuteur.md`](../src/data/prompt-tuteur.md)**

Il n'existe qu'à cet endroit — pas de copie dans `docs/`, pour éviter que les
deux versions divergent. Modifie-le là, et le tuteur change à la séance suivante.
